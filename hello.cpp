#include <iostream>

using namespace std;
int main()
{   
    //for(int a=0; a<5; a++) cout << a << endl;
    int i,k;
    cin >> i;
    cin >> k;
    string l; cin.ignore();getline(cin,l);
    for(int a = 0; a < 5; a++)
        ;
    if(i==1)
    if(k==2)
    if(l=="yes")
    cout << "hello world" << endl;
    return 0;
}